import { Flex, Image } from "antd";
import styled from "styled-components";

export const ImageNotFound = styled(Image)`
  border-radius: 50px;
  -webkit-user-drag: none;
  box-shadow: 0px 8px 10px rgba(0, 0, 0, 0.3);
`;

export const FlexImage = styled(Flex)`
  height: 100vh;
`;
